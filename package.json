{
  "name": "ai-sharepoint",
  "displayName": "AI SharePoint",
  "description": "Govern and explore SharePoint Online with GitHub Copilot — metered usage, enterprise auth, read-only reference sources, and privacy-first diagnostics.",
  "version": "0.68.0",
  "release": { "channel": "standard" },
  "publisher": "alfredsisley10",
  "license": "MIT",
  "qna": false,
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#16243a",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Other"
  ],
  "keywords": [
    "sharepoint",
    "copilot",
    "microsoft 365",
    "governance",
    "enterprise",
    "graph"
  ],
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "virtualWorkspaces": true,
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "Authentication settings (authority, client ID, authority allowlist) are machine-scoped and cannot be overridden by workspace settings in untrusted workspaces.",
      "restrictedConfigurations": [
        "aiSharePoint.auth.tenantAuthority",
        "aiSharePoint.auth.clientId",
        "aiSharePoint.auth.additionalAuthorityHosts",
        "aiSharePoint.sync.allowedRemoteHosts",
        "aiSharePoint.ldap.caCertificatesFile",
        "aiSharePoint.context.allowSchemaIndexing",
        "aiSharePoint.ldap.dnsServers",
        "aiSharePoint.servicenow.oauthClientId"
      ]
    }
  },
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "aiSharePoint.connectSite",
        "title": "Connect SharePoint Site",
        "category": "AI SharePoint",
        "icon": "$(add)"
      },
      {
        "command": "aiSharePoint.addManagedTarget",
        "title": "Add Managed Target (SharePoint or Confluence)",
        "category": "AI SharePoint",
        "icon": "$(add)"
      },
      {
        "command": "aiSharePoint.connectSiteSession",
        "title": "Connect SharePoint Site (browser session — no admin consent)",
        "category": "AI SharePoint",
        "icon": "$(key)"
      },
      {
        "command": "aiSharePoint.disconnectSiteSession",
        "title": "Disconnect SharePoint Browser Session",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.openSourceInBrowser",
        "title": "Open Source in Browser",
        "category": "AI SharePoint",
        "icon": "$(link-external)"
      },
      {
        "command": "aiSharePoint.changeSourceRole",
        "title": "Change Source Role (Managed / Reference)",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.testWriteAccess",
        "title": "Test Write Access (safe — creates & deletes a temp page)",
        "category": "AI SharePoint",
        "icon": "$(beaker)"
      },
      {
        "command": "aiSharePoint.testConfluenceFunctionality",
        "title": "Test Content Functionality (safe — renders & checks page elements)",
        "category": "AI SharePoint",
        "icon": "$(symbol-color)"
      },
      {
        "command": "aiSharePoint.testConnection",
        "title": "Test Site Connection",
        "category": "AI SharePoint",
        "icon": "$(plug)"
      },
      {
        "command": "aiSharePoint.openSiteInBrowser",
        "title": "Open Site in Browser",
        "category": "AI SharePoint",
        "icon": "$(link-external)"
      },
      {
        "command": "aiSharePoint.copySiteUrl",
        "title": "Copy Site URL",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.changeSiteRole",
        "title": "Change Connection Role (managed/reference)",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.signOutSite",
        "title": "Sign Out of Site (wipe cached credentials)",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.removeSite",
        "title": "Remove Site Connection",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.refreshSites",
        "title": "Refresh Sites View",
        "category": "AI SharePoint",
        "icon": "$(refresh)"
      },
      {
        "command": "aiSharePoint.configureSiteRepo",
        "title": "Configure Site Repository (Git)…",
        "category": "AI SharePoint",
        "icon": "$(repo)"
      },
      {
        "command": "aiSharePoint.pullSiteToRepo",
        "title": "Pull Site to Repository (preview first)",
        "category": "AI SharePoint",
        "icon": "$(cloud-download)"
      },
      {
        "command": "aiSharePoint.pushSiteRepo",
        "title": "Push Site Repository to GitHub/GHES",
        "category": "AI SharePoint",
        "icon": "$(repo-push)"
      },
      {
        "command": "aiSharePoint.applyRepoToSharePoint",
        "title": "Apply Repository to SharePoint (write-back)…",
        "category": "AI SharePoint",
        "icon": "$(cloud-upload)"
      },
      {
        "command": "aiSharePoint.revertSiteToCommit",
        "title": "Revert Site to Commit…",
        "category": "AI SharePoint",
        "icon": "$(history)"
      },
      {
        "command": "aiSharePoint.addContextSource",
        "title": "Add Context Source (Confluence/Jira/GitHub/LDAP/Databases/Vertex/Power BI/ServiceNow/Splunk/Observability/Grafana)",
        "category": "AI SharePoint",
        "icon": "$(add)"
      },
      {
        "command": "aiSharePoint.testContextSource",
        "title": "Test Context Source",
        "category": "AI SharePoint",
        "icon": "$(plug)"
      },
      {
        "command": "aiSharePoint.removeContextSource",
        "title": "Remove Context Source",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.editSourceAlias",
        "title": "Edit Alias & Description",
        "category": "AI SharePoint",
        "icon": "$(tag)"
      },
      {
        "command": "aiSharePoint.loadSourceSchema",
        "title": "Load/Refresh Database Schema",
        "category": "AI SharePoint",
        "icon": "$(database)"
      },
      {
        "command": "aiSharePoint.precacheSourceCatalog",
        "title": "Pre-cache Source Catalog (spaces/projects/queues)…",
        "category": "AI SharePoint",
        "icon": "$(cloud-download)"
      },
      {
        "command": "aiSharePoint.draftTeamsMessage",
        "title": "Draft Teams Message…",
        "category": "AI SharePoint",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "aiSharePoint.draftOutlookEmail",
        "title": "Draft Outlook Email…",
        "category": "AI SharePoint",
        "icon": "$(mail)"
      },
      {
        "command": "aiSharePoint.reviewCommDraft",
        "title": "Review & Send Communication Draft…",
        "category": "AI SharePoint",
        "icon": "$(send)"
      },
      {
        "command": "aiSharePoint.editCommDraft",
        "title": "Edit Communication Draft",
        "category": "AI SharePoint",
        "icon": "$(edit)"
      },
      {
        "command": "aiSharePoint.discardCommDraft",
        "title": "Discard Communication Draft",
        "category": "AI SharePoint",
        "icon": "$(trash)"
      },
      {
        "command": "aiSharePoint.configureTeamsWebhook",
        "title": "Configure Teams Webhook (no admin consent)…",
        "category": "AI SharePoint",
        "icon": "$(plug)"
      },
      {
        "command": "aiSharePoint.testCommsMethod",
        "title": "Test Communication Method (end-to-end)…",
        "category": "AI SharePoint",
        "icon": "$(beaker)"
      },
      {
        "command": "aiSharePoint.indexSourceSchema",
        "title": "Index Database Schema (Copilot summaries)…",
        "category": "AI SharePoint",
        "icon": "$(sparkle)"
      },
      {
        "command": "aiSharePoint.buildErDiagram",
        "title": "Build Database ER Diagram (probe join rates)…",
        "category": "AI SharePoint",
        "icon": "$(type-hierarchy)"
      },
      {
        "command": "aiSharePoint.viewSourceSchema",
        "title": "View Database Schema & Semantic Index",
        "category": "AI SharePoint",
        "icon": "$(table)"
      },
      {
        "command": "aiSharePoint.resetSourceLockout",
        "title": "Reset Source Auth Lockout",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.clearContextCache",
        "title": "Clear Reference-Source Cache",
        "category": "AI SharePoint",
        "icon": "$(clear-all)"
      },
      {
        "command": "aiSharePoint.exportSearchResults",
        "title": "Export Context Search Results to File…",
        "category": "AI SharePoint",
        "icon": "$(export)"
      },
      {
        "command": "aiSharePoint.discoverActiveDirectory",
        "title": "Discover Active Directory (DNS)",
        "category": "AI SharePoint",
        "icon": "$(search)"
      },
      {
        "command": "aiSharePoint.exportReferenceConfig",
        "title": "Export Reference Config (secret-free)…",
        "category": "AI SharePoint",
        "icon": "$(export)"
      },
      {
        "command": "aiSharePoint.importReferenceConfig",
        "title": "Import Reference Config…",
        "category": "AI SharePoint",
        "icon": "$(cloud-download)"
      },
      {
        "command": "aiSharePoint.browseSource",
        "title": "Browse Source & Add Bookmark…",
        "category": "AI SharePoint",
        "icon": "$(bookmark)"
      },
      {
        "command": "aiSharePoint.addBookmark",
        "title": "Add Bookmark",
        "category": "AI SharePoint",
        "icon": "$(bookmark)"
      },
      {
        "command": "aiSharePoint.runBookmark",
        "title": "Run Bookmark",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.removeBookmark",
        "title": "Remove Bookmark",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.editBookmark",
        "title": "Edit Bookmark (name & query)",
        "category": "AI SharePoint",
        "icon": "$(edit)"
      },
      {
        "command": "aiSharePoint.installCopilotChat",
        "title": "Install GitHub Copilot Chat",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.checkCopilotStatus",
        "title": "Check Copilot Status",
        "category": "AI SharePoint",
        "icon": "$(pulse)"
      },
      {
        "command": "aiSharePoint.listModels",
        "title": "List Copilot Models (relative cost)",
        "category": "AI SharePoint",
        "icon": "$(circuit-board)"
      },
      {
        "command": "aiSharePoint.askCopilot",
        "title": "Ask Copilot",
        "category": "AI SharePoint",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "aiSharePoint.showUsage",
        "title": "Show Copilot Activity Dashboard",
        "category": "AI SharePoint",
        "icon": "$(dashboard)"
      },
      {
        "command": "aiSharePoint.resetUsage",
        "title": "Reset Copilot Activity Counters",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.exportDiagnostics",
        "title": "Export Diagnostics Bundle (anonymized)",
        "category": "AI SharePoint",
        "icon": "$(export)"
      },
      {
        "command": "aiSharePoint.reviewLessons",
        "title": "Review Lessons Learned (anonymized)",
        "category": "AI SharePoint",
        "icon": "$(book)"
      },
      {
        "command": "aiSharePoint.manageProxyTerms",
        "title": "Manage Proxy Avoid-List",
        "category": "AI SharePoint",
        "icon": "$(shield)"
      },
      {
        "command": "aiSharePoint.showModelLimits",
        "title": "Show Learned Model Context Limits",
        "category": "AI SharePoint",
        "icon": "$(dashboard)"
      },
      {
        "command": "aiSharePoint.manageProjectMemory",
        "title": "Manage Project Memory",
        "category": "AI SharePoint",
        "icon": "$(sparkle)"
      },
      {
        "command": "aiSharePoint.exportLessons",
        "title": "Export Lessons Learned (anonymized)",
        "category": "AI SharePoint",
        "icon": "$(export)"
      },
      {
        "command": "aiSharePoint.clearLessons",
        "title": "Delete Captured Lessons",
        "category": "AI SharePoint",
        "icon": "$(trash)"
      },
      {
        "command": "aiSharePoint.showErrorReports",
        "title": "Show Error Reports",
        "category": "AI SharePoint",
        "icon": "$(bug)"
      },
      {
        "command": "aiSharePoint.clearErrorReports",
        "title": "Delete Error Reports",
        "category": "AI SharePoint",
        "icon": "$(trash)"
      },
      {
        "command": "aiSharePoint.rotateAnonymousId",
        "title": "Rotate Anonymous Install ID",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.openLogs",
        "title": "Open Extension Logs",
        "category": "AI SharePoint",
        "icon": "$(output)"
      },
      {
        "command": "aiSharePoint.rebrandExtension",
        "title": "Rebrand / White-label This Extension…",
        "category": "AI SharePoint",
        "icon": "$(paintcan)"
      },
      {
        "command": "aiSharePoint.openUserGuide",
        "title": "Open User Guide",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.openPrivacyNotice",
        "title": "Open Privacy & Data Notice",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.openWalkthrough",
        "title": "Open Getting Started Walkthrough",
        "category": "AI SharePoint"
      },
      {
        "command": "aiSharePoint.toggleVerboseLogging",
        "title": "Toggle Verbose Wire Logging",
        "category": "AI SharePoint",
        "icon": "$(eye)"
      },
      {
        "command": "aiSharePoint.indexSourceContent",
        "title": "Index Database Content Types (sampled values)…",
        "category": "AI SharePoint",
        "icon": "$(beaker)"
      },
      {
        "command": "aiSharePoint.createProject",
        "title": "Projects: Create Project…",
        "category": "AI SharePoint",
        "icon": "$(new-folder)"
      },
      {
        "command": "aiSharePoint.switchProject",
        "title": "Projects: Switch Project / All Sources",
        "category": "AI SharePoint",
        "icon": "$(folder-opened)"
      },
      {
        "command": "aiSharePoint.editProject",
        "title": "Projects: Edit Project…",
        "category": "AI SharePoint",
        "icon": "$(edit)"
      },
      {
        "command": "aiSharePoint.removeProject",
        "title": "Projects: Remove Project…",
        "category": "AI SharePoint",
        "icon": "$(trash)"
      },
      {
        "command": "aiSharePoint.activateProject",
        "title": "Projects: Activate Project",
        "category": "AI SharePoint",
        "icon": "$(check)"
      },
      {
        "command": "aiSharePoint.manageProjectAiContext",
        "title": "Projects: Manage AI Context…",
        "category": "AI SharePoint",
        "icon": "$(sparkle)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "aiSharePoint",
          "title": "AI SharePoint",
          "icon": "media/activity-icon.svg"
        }
      ]
    },
    "views": {
      "aiSharePoint": [
        {
          "id": "aiSharePoint.sitesView",
          "name": "Managed Sites",
          "icon": "media/activity-icon.svg",
          "contextualTitle": "Managed Sites (SharePoint or Confluence)"
        },
        {
          "id": "aiSharePoint.sourcesView",
          "name": "Reference Sources",
          "icon": "media/activity-icon.svg",
          "contextualTitle": "Reference Sources (read-only)"
        },
        {
          "id": "aiSharePoint.projectsView",
          "name": "Projects",
          "icon": "media/activity-icon.svg",
          "contextualTitle": "Projects (scopes & AI context)"
        },
        {
          "id": "aiSharePoint.usageView",
          "name": "Copilot Activity",
          "icon": "media/activity-icon.svg",
          "contextualTitle": "Copilot Activity (local request counts)"
        },
        {
          "id": "aiSharePoint.commsView",
          "name": "Communications",
          "icon": "media/activity-icon.svg",
          "contextualTitle": "Communications (drafts pending approval)"
        },
        {
          "id": "aiSharePoint.supportView",
          "name": "Support & Diagnostics",
          "icon": "media/activity-icon.svg",
          "contextualTitle": "Support & Diagnostics"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "aiSharePoint.sitesView",
        "contents": "No managed targets yet. Managed targets are the ones @sharepoint can change: SharePoint sites (full pull / apply / revert lifecycle) and Confluence spaces or pages (read/write with your own API token — no admin consent).\n[Add Managed Target](command:aiSharePoint.addManagedTarget)\nSharePoint signs in with your Microsoft account (browser or device-code; Commercial, GCC High and 21Vianet clouds). A managed Confluence target is scoped to the space or page you onboard — but still reads across all of Confluence.\nRead-only sources appear under Reference Sources. Credentials are stored only in your OS keychain. [Privacy & data notice](command:aiSharePoint.openPrivacyNotice)",
        "when": "!aiSharePoint.hasSites"
      },
      {
        "view": "aiSharePoint.usageView",
        "contents": "GitHub Copilot Chat is not installed. AI SharePoint runs all AI features through your own Copilot entitlement — no extra keys.\n[Install GitHub Copilot Chat](command:aiSharePoint.installCopilotChat)\nAfter installing, sign in to GitHub when prompted.",
        "when": "!aiSharePoint.copilotChatInstalled"
      },
      {
        "view": "aiSharePoint.usageView",
        "contents": "Copilot Chat is installed, but no Copilot models are available yet — sign in to GitHub (Accounts menu, bottom-left) with an account that has a Copilot subscription or seat.\n[Check Copilot Status](command:aiSharePoint.checkCopilotStatus)",
        "when": "aiSharePoint.copilotChatInstalled && !aiSharePoint.copilotSignedIn"
      },
      {
        "view": "aiSharePoint.sourcesView",
        "contents": "No reference sources yet. This view holds all read-only context the assistant can search — read-only SharePoint sites plus Confluence, Jira, databases, and more.\n[Add Context Source](command:aiSharePoint.addContextSource)\nRead-only SharePoint sites you connect appear here too. Credentials stay in your OS keychain; every source is strictly read-only with account-lockout protection."
      },
      {
        "view": "aiSharePoint.commsView",
        "contents": "No drafts pending. Prepare a Teams chat or Outlook email — yourself or by asking @sharepoint to draft one — and it waits here until YOU approve sending it.\n[Draft Teams Message](command:aiSharePoint.draftTeamsMessage)\n[Draft Outlook Email](command:aiSharePoint.draftOutlookEmail)\nNothing is ever sent without your per-message approval (ADR-0025)."
      },
      {
        "view": "aiSharePoint.projectsView",
        "contents": "Projects group reference sources & bookmarks, define **goals** and **instructions** for @sharepoint, and keep a separate **AI-managed context** the assistant learns over time.\n[Create Project](command:aiSharePoint.createProject)\nActivate a project to scope chat and the Reference Sources view to it."
      }
    ],
    "chatParticipants": [
      {
        "id": "aiSharePoint.sharepoint",
        "name": "sharepoint",
        "fullName": "SharePoint",
        "description": "Ask about your SharePoint sites, plans, and Copilot usage",
        "isSticky": true,
        "commands": [
          {
            "name": "site",
            "description": "Live overview of a connected site (lists, pages)"
          },
          {
            "name": "sites",
            "description": "List your configured site connections"
          },
          {
            "name": "usage",
            "description": "This extension's Copilot request activity (local counts)"
          },
          {
            "name": "help",
            "description": "How to use @sharepoint"
          }
        ]
      }
    ],
    "languageModelTools": [
      {
        "name": "aisharepoint_list_connections",
        "displayName": "List SharePoint Connections",
        "toolReferenceName": "spConnections",
        "canBeReferencedInPrompt": true,
        "icon": "$(plug)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "List the user's configured SharePoint site connections (name, URL, role, verified). Read-only. Use it to discover which sites are available before other SharePoint tools.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "aisharepoint_site_overview",
        "displayName": "SharePoint Site Overview",
        "toolReferenceName": "spSiteOverview",
        "canBeReferencedInPrompt": true,
        "icon": "$(cloud)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Get a live, read-only overview of a connected SharePoint site: title, description, lists/libraries, and modern pages. Pass the site URL or display name; omit it if only one connection exists.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Site URL or display name of a configured connection. Optional when exactly one connection exists."
            }
          }
        }
      },
      {
        "name": "aisharepoint_inspect_site",
        "displayName": "Inspect Site Architecture (read-only)",
        "toolReferenceName": "spInspectSite",
        "canBeReferencedInPrompt": true,
        "icon": "$(type-hierarchy-sub)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Authoritative, READ-ONLY architecture inspection of a connected SharePoint site — works for reference (read-only) connections exactly like managed ones; no repository, pull, or managed onboarding is ever needed to read. Without 'page': enumerates every visible list/library WITH its columns (name/type/required) plus the page inventory. With page: \"<title or id>\": returns that page's full layout — sections, columns, and each web part (text content stripped of HTML, standard web parts by title/type). Use this for tech-stack, component-by-page, or 'how is this site built' questions.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Site URL or display name of a configured connection. Optional when exactly one connection exists."
            },
            "page": {
              "type": "string",
              "description": "Optional page title (or id) for a full section/web-part breakdown of that page."
            }
          }
        }
      },
      {
        "name": "aisharepoint_scan_site_content",
        "displayName": "Scan Site Content (read-only)",
        "toolReferenceName": "spScanSiteContent",
        "canBeReferencedInPrompt": true,
        "icon": "$(search-fuzzy)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Read-only FULL-SITE content scan: walks EVERY modern page of a connected SharePoint site and returns each page's RENDERED content and web-part inventory — headings, visible text, link targets (Quick Links targets, Hero tiles, Call to action, in-text links), embedded list views, and a site-wide web-part histogram. Use this to review an ENTIRE site for DUPLICATIVE, OUT-OF-DATE, or CONFUSING content and to recommend cleanup (e.g. 'review the entire contents of <site> to identify duplicative, out-of-date, or confusing content for users'). It is also the way to ground a cross-source review: scan the authoritative SharePoint site here, then search another reference source (Confluence, Jira, …) with search_context to find content that conflicts with or is misled by it. Works for reference (read-only) and managed connections alike — no onboarding needed. Defaults to the first 50 pages; pass maxPages (up to 100) to widen. For ONE page's deep section/web-part breakdown use inspect_site with page; for the list/column architecture use inspect_site without page.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Site URL or display name of a configured connection. Optional when exactly one connection exists."
            },
            "maxPages": {
              "type": "number",
              "description": "Maximum modern pages to scan (default 50, max 100)."
            }
          }
        }
      },
      {
        "name": "aisharepoint_list_pages",
        "displayName": "List SharePoint Pages",
        "toolReferenceName": "spPages",
        "canBeReferencedInPrompt": true,
        "icon": "$(files)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "List the modern pages of a connected SharePoint site (title, URL, last modified). Read-only. Pass the site URL or display name; omit it if only one connection exists.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Site URL or display name of a configured connection. Optional when exactly one connection exists."
            }
          }
        }
      },
      {
        "name": "aisharepoint_copilot_usage",
        "displayName": "Copilot Activity",
        "toolReferenceName": "spUsage",
        "canBeReferencedInPrompt": true,
        "icon": "$(graph-line)",
        "tags": [
          "sharepoint",
          "usage"
        ],
        "modelDescription": "Report the requests this extension made through the user's Copilot subscription (factual local counts: today/this month, failures, by model with tokens). Premium-request consumption against the user's plan is NOT tracked — the GitHub billing page is the authoritative source.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "aisharepoint_list_sources",
        "displayName": "List Reference Sources",
        "toolReferenceName": "spSources",
        "canBeReferencedInPrompt": true,
        "icon": "$(library)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "List the user's configured read-only reference sources (Confluence, Jira, GitHub (github.com or Enterprise Server), LDAP/Active Directory, SQL Server/PostgreSQL/MySQL/MongoDB databases, Vertex AI Search, Power BI, ServiceNow, Splunk, Splunk Observability Cloud, Grafana) with their chat alias (the short handle users say, e.g. \"CMDB\"), description of contents, type, deployment, and verification state. Use before searching to discover what is available and map a name the user mentioned to a source.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "aisharepoint_write_confluence_page",
        "displayName": "Write Confluence Page",
        "toolReferenceName": "spWriteConfluencePage",
        "canBeReferencedInPrompt": true,
        "icon": "$(edit)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Create or UPDATE a page in a Confluence space — the writable target that needs NO SharePoint tenant-admin consent (it uses the Confluence source's own API token). Approval-gated: the user must confirm before anything is written. To CREATE: action='create', source=<Confluence source name>, spaceKey, title, markdown, optional parentId. To UPDATE: action='update', source, pageId (find it first via search_context on that source), title, markdown — the page's version is read and bumped automatically. ADVANCED ELEMENTS (table of contents, panels, status, task lists, Jira issue/filter, draw.io, expand, layouts) are Confluence MACROS in storage-format XHTML — call confluence_capabilities first to see the vocabulary and what's installed, then pass the storage XHTML as 'markdown' WITH format='storage'. NEVER write wiki/markdown shorthand like '[TOC]' or '{toc}' — Confluence renders those as the literal text, not a real element (use <ac:structured-macro ac:name=\"toc\"/>). With format='markdown' (default) the body auto-converts: fenced code → code macro, '- [ ]' → task list, '---' → rule, a stray '[TOC]' → toc macro. DATA FORMAT: the body is Confluence STORAGE FORMAT — well-formed XHTML — so when using format='storage' write valid XHTML: escape '&' as '&amp;', self-close void tags ('<br/>', '<hr/>', '<img …/>'), and close every tag. The 'title' is PLAIN TEXT: no HTML tags and no entities — write 'R&D', never 'R&amp;D' or '<b>R&D</b>'. (The connector auto-corrects bare '&', unclosed void tags and stray title HTML, but emit clean data anyway.) After a write, call validate_confluence_page with the returned pageId to confirm the elements rendered. Returns the saved page's title, new version, and URL; Confluence keeps version history, so updates are reversible. Use ONLY for a change the user explicitly asked to make.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one Confluence source is configured)."
            },
            "action": {
              "type": "string",
              "enum": [
                "create",
                "update"
              ],
              "description": "Create a new page or update an existing one."
            },
            "spaceKey": {
              "type": "string",
              "description": "Target space key (create only)."
            },
            "title": {
              "type": "string",
              "description": "Page title."
            },
            "markdown": {
              "type": "string",
              "description": "Page body. Markdown (auto-converted) OR Confluence storage-format XHTML when format='storage' (for macros/advanced elements)."
            },
            "format": {
              "type": "string",
              "enum": [
                "markdown",
                "storage"
              ],
              "description": "How to treat 'markdown': 'markdown' (default, auto-converted) or 'storage' (raw Confluence storage XHTML, for macros like toc/panels/jira/drawio/task-lists). Bodies already containing <ac:…> macros are treated as storage automatically."
            },
            "pageId": {
              "type": "string",
              "description": "Existing page id (update only) — find it via search_context on the source."
            },
            "parentId": {
              "type": "string",
              "description": "Optional parent page id (create only)."
            }
          },
          "required": [
            "title",
            "markdown"
          ]
        }
      },
      {
        "name": "aisharepoint_confluence_capabilities",
        "displayName": "Confluence Content Capabilities",
        "toolReferenceName": "spConfluenceCapabilities",
        "canBeReferencedInPrompt": true,
        "icon": "$(symbol-color)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Discover Confluence's 'Add more content' vocabulary so you can design advanced pages with REAL elements. Returns (a) the macro/element catalog you can author — code/markup, horizontal rule, status, info/note/warning/tip panels, expand, table of contents, anchor, task lists, multi-column layouts, children/pagetree/include/excerpt, Jira issue/filter, draw.io/Gliffy/Mermaid/PlantUML diagrams, images — each with its params, body type and owning app; (b) the elements EMPIRICALLY in use in a sampled space/page (the reliable signal of what's installed here); and (c) a best-effort list of installed apps. Call this BEFORE authoring a page with macros, then write the storage-format XHTML via write_confluence_page with format='storage'. Args: source (Confluence source name; optional if one), and optionally spaceKey or pageId (+subtree) to scope the sample; defaults to the connector's managed space.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "spaceKey": {
              "type": "string",
              "description": "Sample this space for elements in use (optional)."
            },
            "pageId": {
              "type": "string",
              "description": "Sample this page (optional)."
            },
            "subtree": {
              "type": "boolean",
              "description": "With pageId, sample the page's descendants too."
            }
          }
        }
      },
      {
        "name": "aisharepoint_validate_confluence_page",
        "displayName": "Validate Rendered Confluence Page",
        "toolReferenceName": "spValidateConfluencePage",
        "canBeReferencedInPrompt": true,
        "icon": "$(check-all)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Pull a Confluence page's TRUE RENDERED content (body.view, macros expanded) and validate that elements rendered as intended. Flags wiki/markdown shorthand that LEAKED as visible text — e.g. a literal '[TOC]' or '{info}' that never became a real element — and inventories the macros that actually rendered. Call this AFTER write_confluence_page (pass the returned pageId) as the confirmation step; if it reports leaks, re-publish those elements as real storage-format macros (format='storage'). Args: source (Confluence source name; optional if one), pageId (required).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "pageId": {
              "type": "string",
              "description": "The page to validate (required) — typically the id returned by write_confluence_page."
            }
          },
          "required": [
            "pageId"
          ]
        }
      },
      {
        "name": "aisharepoint_manage_confluence_labels",
        "displayName": "Manage Confluence Page Labels",
        "toolReferenceName": "spConfluenceLabels",
        "canBeReferencedInPrompt": true,
        "icon": "$(tag)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "List, ADD, or REMOVE labels on a Confluence page. action='list' reads the page's labels (no approval). action='add' or 'remove' changes them — approval-gated, and bounded by the connector's write scope (the page must be in the managed space/page). Provide source (Confluence source name; optional if one), pageId (find it via search_context), and for add/remove a 'labels' array. Labels are normalized to Confluence's rules (lowercased, spaces → hyphens). Returns the page's labels after the change. Use this when the user asks to label/tag/categorize a page or to remove a label.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "pageId": {
              "type": "string",
              "description": "The page whose labels to manage (required)."
            },
            "action": {
              "type": "string",
              "enum": [
                "list",
                "add",
                "remove"
              ],
              "description": "list (read), add, or remove labels. Defaults to list."
            },
            "labels": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Labels to add or remove (required for add/remove)."
            }
          },
          "required": [
            "pageId"
          ]
        }
      },
      {
        "name": "aisharepoint_archive_confluence_page",
        "displayName": "Archive Confluence Page",
        "toolReferenceName": "spArchiveConfluence",
        "canBeReferencedInPrompt": true,
        "icon": "$(archive)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Archive a Confluence page: move it under the space's \"Archive\" root page (created automatically if it doesn't exist). The usual FIRST cleanup step for out-of-date content (often followed later by remove-from-search). Approval-gated write, bounded by the connector's write scope. Reversible — the page is relocated, not deleted. Args: source (Confluence source name; optional if one), pageId (find via search_context). Returns the archive root it was moved under.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "pageId": {
              "type": "string",
              "description": "The page to archive (required)."
            }
          },
          "required": [
            "pageId"
          ]
        }
      },
      {
        "name": "aisharepoint_move_confluence_page",
        "displayName": "Move / Re-parent Confluence Page",
        "toolReferenceName": "spMoveConfluence",
        "canBeReferencedInPrompt": true,
        "icon": "$(type-hierarchy)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Move or re-order a Confluence page in the tree. TWO modes: (1) RE-PARENT — make the page a child of another page: position='append' (the default) with parentId=<new parent page id>. (2) RE-ORDER under the same parent — place the page before or after a sibling: position='before' or 'after' with targetId=<sibling page id>. Approval-gated write; BOTH the page and the target must be within the connector's managed scope (so a page can't be moved out of the managed space). Find page ids via search_context. Args: source (optional if one), pageId (the page to move), and either parentId (re-parent) or position+targetId (re-order). Returns the page's new parent.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "pageId": {
              "type": "string",
              "description": "The page to move (required)."
            },
            "parentId": {
              "type": "string",
              "description": "Re-parent under this page id (used with position='append', the default)."
            },
            "position": {
              "type": "string",
              "enum": [
                "append",
                "before",
                "after"
              ],
              "description": "append = make a child of parentId (re-parent, default); before/after = reorder relative to targetId sibling."
            },
            "targetId": {
              "type": "string",
              "description": "The sibling page id to place this page before/after (used with position='before'|'after')."
            }
          },
          "required": [
            "pageId"
          ]
        }
      },
      {
        "name": "aisharepoint_remove_confluence_page_from_search",
        "displayName": "Remove Confluence Page From Search",
        "toolReferenceName": "spRemoveFromSearch",
        "canBeReferencedInPrompt": true,
        "icon": "$(eye-closed)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Remove a Confluence page from search by BLANKING its current content, so it drops out of search and navigation. The page is NEVER deleted — Confluence keeps every prior version, so the original content is retained for compliance and is restorable. This is the step taken AFTER archiving when a page still shouldn't surface. Approval-gated write, bounded by the connector's write scope. Args: source (optional if one), pageId. Only do this when the user explicitly asks to remove a page from search / hide it.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "pageId": {
              "type": "string",
              "description": "The page to remove from search (required)."
            }
          },
          "required": [
            "pageId"
          ]
        }
      },
      {
        "name": "aisharepoint_resolve_page_owners",
        "displayName": "Resolve Confluence Page Owners",
        "toolReferenceName": "spPageOwners",
        "canBeReferencedInPrompt": true,
        "icon": "$(person)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Determine who OWNS a Confluence page: the owner label if one is set, otherwise the most prolific contributor on the page, otherwise in the space. Read-only. Use to find who to notify about a page (e.g. for cleanup of inaccurate content). NOTE: active-user filtering needs an LDAP/M365 directory which isn't wired, so owners are ranked by contribution volume, not filtered by who is still employed/active. Args: source (optional if one), pageId.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "pageId": {
              "type": "string",
              "description": "The page whose owners to resolve (required)."
            }
          },
          "required": [
            "pageId"
          ]
        }
      },
      {
        "name": "aisharepoint_review_space_manageability",
        "displayName": "Review Confluence Space Manageability",
        "toolReferenceName": "spManageability",
        "canBeReferencedInPrompt": true,
        "icon": "$(verified)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Audit whether the signed-in user can READ and WRITE every page in a Confluence space — the usual blocker to a cleanup pass is page-level restrictions. Returns the pages the user can't fully manage (with what's missing) and a ready-to-send access-request note for the space admins. Read-only. Args: source (optional if one), spaceKey (optional — defaults to the connector's managed space).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "spaceKey": {
              "type": "string",
              "description": "Space to audit (optional — defaults to the connector's managed space)."
            }
          }
        }
      },
      {
        "name": "aisharepoint_review_page_currency",
        "displayName": "Review Confluence Page Currency",
        "toolReferenceName": "spPageCurrency",
        "canBeReferencedInPrompt": true,
        "icon": "$(history)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Review a Confluence page's currency: check its outbound links for dead/broken ones (HEAD/GET, bounded), report the owner tag, and the page's age (flagging >365 days stale). Read-only. Use when asked to check if a page is still up to date. NOTE: owner-ACTIVITY verification needs an LDAP/M365 directory (not wired) — owner names are reported as-is. Args: source (optional if one), pageId.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "pageId": {
              "type": "string",
              "description": "The page to review (required)."
            }
          },
          "required": [
            "pageId"
          ]
        }
      },
      {
        "name": "aisharepoint_confluence_page_tree",
        "displayName": "Confluence Page Hierarchy",
        "toolReferenceName": "spConfluenceTree",
        "canBeReferencedInPrompt": true,
        "icon": "$(list-tree)",
        "tags": [
          "sharepoint",
          "confluence"
        ],
        "modelDescription": "Understand a Confluence page's PLACE IN THE TREE and its relationships — use this whenever you need a page's parent, ancestors, children, or whole subtree (search_context finds pages but NOT how they relate). All listings are fully paginated, so enumeration is complete, not truncated. Views (the 'view' arg, with a pageId): 'context' (default) = the breadcrumb (ancestors), the immediate parent, AND the immediate children, in one read; 'ancestors' = just the breadcrumb path; 'children' = just the immediate child pages; 'subtree' = the full nested descendant tree (indented outline). With a spaceKey and NO pageId, returns the space's ROOT pages (the top of the tree). Each page is returned with its id, title and URL so you can drill in or move/re-parent it. Args: source (optional if one), pageId OR spaceKey, optional view.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Confluence source name/alias (optional when only one is configured)."
            },
            "pageId": {
              "type": "string",
              "description": "The page whose hierarchy to read (parent/ancestors/children/subtree)."
            },
            "spaceKey": {
              "type": "string",
              "description": "List this space's root pages (use instead of pageId for the top of the tree)."
            },
            "view": {
              "type": "string",
              "enum": [
                "context",
                "ancestors",
                "children",
                "subtree"
              ],
              "description": "With a pageId: context (ancestors + parent + children, default), ancestors, children, or subtree (full nested tree)."
            }
          }
        }
      },
      {
        "name": "aisharepoint_search_context",
        "displayName": "Search Reference Sources",
        "toolReferenceName": "spSearchContext",
        "canBeReferencedInPrompt": true,
        "icon": "$(search)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Search a configured read-only reference source. Confluence: raw CQL or plain text. Jira: raw JQL or plain text. GitHub (github.com or Enterprise Server): plain text searches issues & pull requests; prefix 'code:', 'repos:', or 'commits:' to search those corpora instead; or JSON {\"type\": \"code|issues|repositories|commits\", \"q\": \"...\", \"limit\": n} (q accepts GitHub's own qualifiers like repo:owner/name, org:, is:open, language:, author:). LDAP/Active Directory: raw LDAP filter or plain text (AD ANR). Databases (SQL Server/PostgreSQL/MySQL): a single read-only SELECT statement (write keywords are blocked). MongoDB: JSON {\"collection\",\"filter\",\"projection\",\"limit\"}. Returns capped, cached results. Pass the source name when several are configured. If a query proves useful for recurring work, propose saving it with the suggest-bookmark tool (user-approved). Vertex AI Search sources: plain-text queries return enterprise search hits (use the vertex_answer tool for synthesized answers). Power BI sources: JSON {\"dataset\": \"<id or name>\", \"dax\": \"EVALUATE …\"} runs a read-only DAX query (use db_schema-style starter EVALUATE INFO.TABLES() to discover tables). ServiceNow sources: plain text (text-index search of the default table), a native encoded query (\"active=true^priority=1\"), or JSON {\"table\": \"cmdb_ci_appl\", \"query\": \"...\", \"fields\": [...], \"limit\": n}. Splunk sources: plain keywords (default index, last 24h), raw SPL (\"search index=web error | stats count by host\", \"| tstats ...\"), or JSON {\"spl\": \"...\", \"earliest\": \"-7d\", \"latest\": \"now\", \"limit\": n} — write/exfil commands (delete/collect/outputlookup/sendemail/script) are blocked. Splunk Observability Cloud sources: plain text searches the configured default object type, or JSON {\"type\": \"metric|dimension|detector|dashboard|incident\", \"query\": \"cpu\", \"limit\": n} (incidents = alerts firing now). Grafana sources: plain text searches dashboards, or JSON {\"type\": \"dashboard|folder|alert|annotation|datasource|panel\", \"query\": \"...\", \"limit\": n, \"folderUid\": \"...\"} — alert returns unified-alerting rule STATE (firing/pending/inactive); panel returns LIVE PANEL DATA by running a dashboard panel's own queries via /api/ds/query and summarizing each series (last/min/max), via {\"type\":\"panel\",\"query\":\"<dashboard uid or title>\",\"panel\":\"<panel id or title>\",\"from\":\"now-6h\"} — so the Grafana connector reads real metric/series values, not only dashboard metadata. SQL Server statements are COST-GATED: table sizes and leading index columns are read from catalog stats first (instant), and a statement estimated to scan a big table runs against a bounded TOP-sample instead — the first result then says so and lists per-table row counts (use those instead of COUNT(*) to size tables). To run the original statement in full, set allowExpensive=true ONLY after the user explicitly accepts a potentially slow scan. Results are capped for chat — when the user wants the dataset itself, use aisharepoint_export_context_results instead (writes up to 50k rows to a workspace file without loading them into context).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Source alias (e.g. \"CMDB\"), display name, or type — as the user said it. Optional when exactly one source exists."
            },
            "query": {
              "type": "string",
              "description": "CQL/JQL or free text."
            },
            "allowExpensive": {
              "type": "boolean",
              "description": "SQL Server only: bypass the cost guard and run the statement in full. Set true ONLY after the user explicitly accepted a slow full-table scan."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "aisharepoint_export_context_results",
        "displayName": "Export Search Results to Workspace File",
        "toolReferenceName": "spExportResults",
        "canBeReferencedInPrompt": true,
        "icon": "$(export)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Run a reference-source query and write EVERY result (up to 50,000 rows, 120s budget) to a file in the workspace — CSV for tabular sources, JSON for MongoDB — returning ONLY the file path and row count. Use whenever the user wants the data itself (a dataset, 'all rows', exports for Excel/analysis) rather than an answer: the normal search tool caps results for chat, this tool bypasses that cap WITHOUT loading the data into context. Same read-only query shapes as the search tool (SELECT for SQL databases, MongoDB JSON spec, CQL/JQL/LDAP/SPL/encoded queries or free text elsewhere). The user approves in a confirmation dialog; for SQL Server the cost guard is bypassed by that approval. Never paste the exported data into chat — point the user at the file.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Source alias (e.g. \"CMDB\"), display name, or type — as the user said it. Optional when exactly one source exists."
            },
            "query": {
              "type": "string",
              "description": "The query to export, in the source's native shape (read-only SELECT, MongoDB JSON spec, CQL/JQL/SPL, or free text)."
            },
            "fileName": {
              "type": "string",
              "description": "Optional file name (no folders); the right extension is enforced and the file lands in ai-sharepoint-exports/."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "aisharepoint_get_context_item",
        "displayName": "Get Reference Item",
        "toolReferenceName": "spContextItem",
        "canBeReferencedInPrompt": true,
        "icon": "$(file)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Fetch one item from a reference source with plain-text body: a Confluence page by id, a Jira issue by key (e.g. ENG-42), or an LDAP/Active Directory entry by distinguished name (DN). Databases have no item fetch — use search with a SELECT statement instead. Read-only. ServiceNow: fetch one record as \"table/sys_id\". GitHub: \"owner/repo#123\" (issue/PR), \"owner/repo@<sha>\" (commit), \"owner/repo:path/to/file\" (file, optional @ref), or \"owner/repo\" (repository).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Source alias (e.g. \"CMDB\"), display name, or type — as the user said it. Optional when exactly one source exists."
            },
            "id": {
              "type": "string",
              "description": "Confluence page id, Jira issue key, or LDAP distinguished name (DN)."
            }
          },
          "required": [
            "id"
          ]
        }
      },
      {
        "name": "aisharepoint_list_bookmarks",
        "displayName": "List Bookmarks",
        "toolReferenceName": "spBookmarks",
        "canBeReferencedInPrompt": true,
        "icon": "$(bookmark)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "List the user's saved reference bookmarks (named, reusable queries or item locators) with their source and kind. Use before running a bookmark by name.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "aisharepoint_run_bookmark",
        "displayName": "Run Bookmark",
        "toolReferenceName": "spRunBookmark",
        "canBeReferencedInPrompt": true,
        "icon": "$(play)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Run a saved reference bookmark by name: executes its stored query (search) or fetches its stored item against the bookmark's source. Read-only, cached, capped.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "The bookmark name."
            }
          },
          "required": [
            "name"
          ]
        }
      },
      {
        "name": "aisharepoint_suggest_bookmark",
        "displayName": "Suggest Bookmark",
        "toolReferenceName": "spSuggestBookmark",
        "canBeReferencedInPrompt": true,
        "icon": "$(bookmark)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "After searching the reference sources (Confluence, Jira, LDAP/AD) and identifying a query or item the user is likely to reuse — e.g. a Jira queue/JQL for their initiative, a Confluence space query, a person's directory entry — propose persisting it as a named bookmark. The user approves via a confirmation dialog before anything is saved; nothing persists without approval. Provide a short, descriptive name, the exact locator (raw CQL/JQL/LDAP filter for kind=query; page id/issue key/DN for kind=item), and a one-line reason.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Source alias (e.g. \"CMDB\"), display name, or type — as the user said it. Optional when exactly one source exists."
            },
            "name": {
              "type": "string",
              "description": "Short descriptive bookmark name."
            },
            "locator": {
              "type": "string",
              "description": "CQL/JQL/LDAP filter (query) or page id / issue key / DN (item)."
            },
            "kind": {
              "type": "string",
              "enum": [
                "query",
                "item"
              ],
              "description": "Defaults to query."
            },
            "reason": {
              "type": "string",
              "description": "One line on why this is worth bookmarking (shown to the user)."
            }
          },
          "required": [
            "name",
            "locator"
          ]
        }
      },
      {
        "name": "aisharepoint_db_schema",
        "displayName": "Database Schema",
        "toolReferenceName": "spDbSchema",
        "canBeReferencedInPrompt": true,
        "icon": "$(database)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Look up a database source's schema before writing SQL for a free-form question. Returns tables and columns (names + types), and — when the schema has been semantically indexed — concept tags and synonyms per column (e.g. a column group_cio tagged 'ownership', aka 'owner/CIO'), so questions like 'records owned by X' map to the right columns. Pass a topic ('ownership', 'application servers', the user's phrasing) to get only matching tables; omit it to list tables. The catalog is loaded automatically on first use (metadata only). Then run your SELECT with the search tool.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Database source alias (e.g. \"CMDB\"), display name, or type. Optional when exactly one source exists."
            },
            "topic": {
              "type": "string",
              "description": "Concept to find columns for — e.g. 'ownership', 'owned by', 'cost', 'application'. Omit to list tables."
            }
          }
        }
      },
      {
        "name": "aisharepoint_vertex_answer",
        "displayName": "Vertex AI Search Answer",
        "toolReferenceName": "spVertexAnswer",
        "canBeReferencedInPrompt": true,
        "icon": "$(search)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Ask a configured Vertex AI Search source (Google enterprise search) for a Gemini-grounded ANSWER with citations over its indexed corpus — use for analytical questions ('summarize our policy on X') where synthesized analysis beats raw hits. For raw result lists use the regular search tool against the same source. Read-only.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Vertex AI Search source alias, display name, or type. Optional when exactly one source exists."
            },
            "query": {
              "type": "string",
              "description": "The question to answer over the enterprise corpus."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "aisharepoint_draft_communication",
        "displayName": "Draft Communication (Teams/Outlook)",
        "toolReferenceName": "spDraftComm",
        "canBeReferencedInPrompt": true,
        "icon": "$(mail)",
        "tags": [
          "sharepoint",
          "communications"
        ],
        "modelDescription": "PREPARE a message to specific individuals (you never SEND). Two paths by channel. OUTLOOK email: the draft is created DIRECTLY in the user's Outlook Drafts folder — Outlook is the review-and-send surface, so there is no in-plugin approval and nothing is sent; tell the user to review and send it from Outlook. TEAMS: Teams posts live (no draft folder), so it is queued in the extension's Communications outbox behind the user's explicit per-draft approval there; tell them it awaits approval in the Communications view, and never say it was saved into Teams or already sent. When the message should reflect existing context (status update, reply, summary), FIRST ground it: search relevant sources — especially a Microsoft 365 Copilot source (Teams messages, email, documents) — then draft from what you find. Provide recipients as emails/UPNs (max 10), a subject for email, and a plain-text body. Use ONLY for a message the user explicitly asked to prepare, addressed to recipients THEY named.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "channel": {
              "type": "string",
              "enum": [
                "teams",
                "outlook"
              ],
              "description": "teams = chat message, outlook = email."
            },
            "to": {
              "type": "string",
              "description": "Recipient emails/UPNs, comma-separated (individuals, max 10)."
            },
            "subject": {
              "type": "string",
              "description": "Email subject (required for outlook, ignored for teams)."
            },
            "body": {
              "type": "string",
              "description": "Plain-text message body."
            },
            "reason": {
              "type": "string",
              "description": "One line on why this communication is being prepared (shown to the user)."
            }
          },
          "required": [
            "channel",
            "to",
            "body"
          ]
        }
      },
      {
        "name": "aisharepoint_index_db_schema",
        "displayName": "Index Database Schema with Copilot",
        "toolReferenceName": "spIndexDbSchema",
        "canBeReferencedInPrompt": true,
        "icon": "$(sparkle)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Build (or rebuild) the semantic index of a database source's schema using the user's Copilot entitlement: table/column NAMES ONLY are sent (never data rows), batched and metered. The user approves in a confirmation dialog first. Offer this when db_schema reports no semantic index and the user asks free-form questions about the database's records — afterwards concepts like ownership ('owned by X') resolve to the right columns (e.g. group_cio).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Database source alias, display name, or type. Optional when exactly one source exists."
            }
          }
        }
      },
      {
        "name": "aisharepoint_set_result_window",
        "displayName": "Set Result Window",
        "toolReferenceName": "spResultWindow",
        "canBeReferencedInPrompt": true,
        "icon": "$(list-ordered)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Tune the result window for THIS conversation — the maximum number of rows/results each reference-source query (database, Confluence, Jira, search) returns. Default is 25 (configurable), hard maximum 200. Call with `size` to raise/lower it when the user wants more or fewer results (e.g. 'show me up to 100 rows'); call with `reset: true` to revert to the configured default; call with no arguments to report the current window. The change lasts for the session only.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "size": {
              "type": "number",
              "description": "New result window (1–200). Clamped to the maximum if higher."
            },
            "reset": {
              "type": "boolean",
              "description": "true = revert to the configured default window."
            }
          }
        }
      },
      {
        "name": "aisharepoint_test_join",
        "displayName": "Test / Save a Database Join",
        "toolReferenceName": "spTestJoin",
        "canBeReferencedInPrompt": true,
        "icon": "$(type-hierarchy)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Validate a user-supplied database join against the persisted ER model, or probe its real join rate (both directions; counts only, never row data). Accepts SQL join syntax (\"FROM Orders o JOIN Customers c ON o.customer_id = c.id\") or a bare equality (\"dbo.Orders.customer_id = dbo.Customers.id\"). If the join is already in the ER model, returns the stored relationship. Set save=true ONLY when the user wants the ER diagram extended with this join — they approve in a confirmation dialog; user-defined joins persist even below the automatic thresholds, with measured rates kept visible.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "description": "Database source alias, display name, or type. Optional when exactly one source exists."
            },
            "join": {
              "type": "string",
              "description": "The join — SQL fragment with ON clause, or table.column = table.column."
            },
            "save": {
              "type": "boolean",
              "description": "true = persist the join into the ER model after probing (user confirms)."
            }
          },
          "required": [
            "join"
          ]
        }
      },
      {
        "name": "aisharepoint_write_site_files",
        "displayName": "Write Site Spec Files",
        "toolReferenceName": "spWriteSiteFiles",
        "canBeReferencedInPrompt": true,
        "icon": "$(edit)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Write lists/<name>.json and pages/<name>.json spec files into a managed site's local repository (the same format pull_site produces — mirror pulled files for structure). Local only: nothing reaches SharePoint until apply_site is approved. Use this to IMPLEMENT a site design you produced, then call apply_site. Requires the site to have a configured repository.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Managed site URL or display name. Optional when exactly one managed site exists."
            },
            "files": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "path": {
                    "type": "string",
                    "description": "lists/<name>.json or pages/<name>.json"
                  },
                  "content": {
                    "type": "string",
                    "description": "JSON file content"
                  }
                },
                "required": [
                  "path",
                  "content"
                ]
              }
            },
            "reason": {
              "type": "string",
              "description": "One line on what this implements (shown to the user)."
            }
          },
          "required": [
            "files"
          ]
        }
      },
      {
        "name": "aisharepoint_pull_site",
        "displayName": "Pull Site to Repository",
        "toolReferenceName": "spPullSite",
        "canBeReferencedInPrompt": true,
        "icon": "$(cloud-download)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Launch the preview-first pull of a managed site's live state into its local repository (no SharePoint writes). Use before writing spec files when the repo may be stale, and to learn the exact file format. The user confirms the preview.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Managed site URL or display name. Optional when exactly one managed site exists."
            }
          }
        }
      },
      {
        "name": "aisharepoint_apply_site",
        "displayName": "Apply Repository to SharePoint",
        "toolReferenceName": "spApplySite",
        "canBeReferencedInPrompt": true,
        "icon": "$(cloud-upload)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Launch the guarded write-back of a managed site's repository to SharePoint: the user sees the operation-level preview, deletions are opt-in, a safety snapshot is taken, and NOTHING changes until they approve the dialog. Call after write_site_files. Never state the site changed until the user's approval completed — verify with site_overview afterwards.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Managed site URL or display name. Optional when exactly one managed site exists."
            }
          }
        }
      },
      {
        "name": "aisharepoint_remember_project_context",
        "displayName": "Remember Project Context",
        "toolReferenceName": "spRemember",
        "canBeReferencedInPrompt": true,
        "icon": "$(sparkle)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Persist a durable, project-specific learning into the ACTIVE project's AI-managed context (separate from the user's own goals/instructions) so it carries across sessions — e.g. 'the user wants app-ownership answers from the CMDB, citing sys_id', 'always answer in German'. The user approves each note in a confirmation dialog. Saving is dedup-aware: a near-duplicate of an existing learning is merged/reinforced rather than duplicated. Use sparingly for genuinely durable behavior the user taught you, not per-turn details. No-op if no project is active.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "note": {
              "type": "string",
              "description": "One concise learning to remember for this project."
            }
          },
          "required": [
            "note"
          ]
        }
      },
      {
        "name": "aisharepoint_forget_project_context",
        "displayName": "Forget Project Context",
        "toolReferenceName": "spForget",
        "canBeReferencedInPrompt": true,
        "icon": "$(trash)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Remove a saved learning from the ACTIVE project's AI-managed context when the user says a remembered behavior is wrong or no longer applies — e.g. 'forget that I want answers in German', 'stop citing the CMDB'. Pass text matching the learning to remove (quote the saved note shown in the project's AI-managed context for an exact match). The user approves the removal in a confirmation dialog. Only affects AI-managed memory, never the user's own goals/instructions. No-op if no project is active.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "note": {
              "type": "string",
              "description": "Text identifying the learning(s) to forget (matched by similarity/containment)."
            }
          },
          "required": [
            "note"
          ]
        }
      },
      {
        "name": "aisharepoint_sp_list_items",
        "displayName": "Read SharePoint List (browser session)",
        "toolReferenceName": "spListItems",
        "canBeReferencedInPrompt": true,
        "icon": "$(table)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Read items from a SharePoint list using the user's BROWSER SESSION (the no-admin write path — only works after the user runs 'Connect SharePoint Site (browser session)'). Returns the items as JSON; the field KEYS are INTERNAL SharePoint field names (e.g. 'Title', 'Due_x0020_Date') — you MUST use those exact keys when writing with sp_write_item. Args: site (site URL or web title; optional if one session is connected), list (the list's display title, required), optional select (comma-separated internal field names), filter (OData $filter), top (max items). Use this to discover the field names before any write.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Site URL or web title of a connected browser session (optional when only one is connected)."
            },
            "list": {
              "type": "string",
              "description": "The list's display title (required)."
            },
            "select": {
              "type": "string",
              "description": "Comma-separated internal field names to return (optional)."
            },
            "filter": {
              "type": "string",
              "description": "OData $filter expression (optional), e.g. \"Status eq 'Open'\"."
            },
            "top": {
              "type": "number",
              "description": "Max items to return (default 50, cap 500)."
            }
          },
          "required": [
            "list"
          ]
        }
      },
      {
        "name": "aisharepoint_sp_write_item",
        "displayName": "Write SharePoint List Item (browser session)",
        "toolReferenceName": "spWriteItem",
        "canBeReferencedInPrompt": true,
        "icon": "$(edit)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Create or UPDATE a SharePoint list item using the user's BROWSER SESSION — the no-admin write path (needs a connected session; see sp_list_items). Approval-gated: the user confirms before the write. 'fields' is an object keyed by INTERNAL SharePoint field names (read the list first with sp_list_items to get them; e.g. {\"Title\":\"X\",\"Status\":\"Open\"}). To CREATE: action='create', site, list, fields. To UPDATE: action='update', site, list, itemId (from sp_list_items), fields (only the fields to change). Use ONLY for a change the user explicitly asked for. This writes under the user's own SharePoint permissions, so it can only do what they can do in the Web UI.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Site URL or web title of a connected browser session (optional when only one is connected)."
            },
            "list": {
              "type": "string",
              "description": "The list's display title (required)."
            },
            "action": {
              "type": "string",
              "enum": [
                "create",
                "update"
              ],
              "description": "Create a new item or update an existing one."
            },
            "itemId": {
              "type": "number",
              "description": "The item's Id (update only) — find it via sp_list_items."
            },
            "fields": {
              "type": "object",
              "description": "Object keyed by INTERNAL field names → values (required)."
            }
          },
          "required": [
            "list",
            "fields"
          ]
        }
      },
      {
        "name": "aisharepoint_sp_library_files",
        "displayName": "SharePoint Files (browser session)",
        "toolReferenceName": "spFiles",
        "canBeReferencedInPrompt": true,
        "icon": "$(files)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Browse, read, upload, or delete files in a SharePoint document library using the user's BROWSER SESSION (needs a connected session — see sp_list_items). action='list' returns the files & sub-folders under a folder (folder = a server-relative path like '/sites/Eng/Shared Documents' OR a library display title, which is resolved to its root). action='read' returns a text file's content (file = the ServerRelativeUrl from a list result; for text/markdown/csv/json/xml). action='upload' creates or OVERWRITES a text file (folder + fileName + content; approval-gated). action='delete' removes a file (file = ServerRelativeUrl; approval-gated). Uploads/reads handle TEXT content; binary files aren't supported here. Always list first to get exact ServerRelativeUrls.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Site URL or web title of a connected session (optional when only one is connected)."
            },
            "action": {
              "type": "string",
              "enum": [
                "list",
                "read",
                "upload",
                "delete"
              ],
              "description": "list/read are read-only; upload/delete write (approval-gated)."
            },
            "folder": {
              "type": "string",
              "description": "For list/upload: a server-relative folder path (starts with /) or a library display title."
            },
            "file": {
              "type": "string",
              "description": "For read/delete: the file's ServerRelativeUrl (from a list result)."
            },
            "fileName": {
              "type": "string",
              "description": "For upload: the file name to create (e.g. notes.md)."
            },
            "content": {
              "type": "string",
              "description": "For upload: the text content of the file."
            }
          },
          "required": [
            "action"
          ]
        }
      },
      {
        "name": "aisharepoint_sp_manage_page",
        "displayName": "SharePoint Pages (browser session)",
        "toolReferenceName": "spPages",
        "canBeReferencedInPrompt": true,
        "icon": "$(browser)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "List, read, or create modern SharePoint pages using the user's BROWSER SESSION (needs a connected session — see sp_list_items). action='list' returns the site's pages (Id, Title, Url). action='read' returns a page's title + readable text (pageId from the list). action='create' creates AND PUBLISHES a new modern page with a single rich-text web part (title + body; body may be plain text or HTML; approval-gated). Note: modern-page authoring via the REST canvas is SharePoint-version sensitive and best-effort — if a create fails it reports why. Reading/listing are reliable.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "site": {
              "type": "string",
              "description": "Site URL or web title of a connected session (optional when only one is connected)."
            },
            "action": {
              "type": "string",
              "enum": [
                "list",
                "read",
                "create"
              ],
              "description": "list/read are read-only; create writes & publishes (approval-gated)."
            },
            "pageId": {
              "type": "number",
              "description": "For read: the page Id (from the list action)."
            },
            "title": {
              "type": "string",
              "description": "For create: the page title."
            },
            "body": {
              "type": "string",
              "description": "For create: the page body (plain text or HTML)."
            }
          },
          "required": [
            "action"
          ]
        }
      },
      {
        "name": "aisharepoint_avoid_term",
        "displayName": "Manage Proxy Avoid-List",
        "toolReferenceName": "spAvoidTerm",
        "canBeReferencedInPrompt": true,
        "icon": "$(shield)",
        "tags": [
          "sharepoint"
        ],
        "modelDescription": "Manage the words-to-avoid list for a corporate proxy that blocks LLM requests/responses containing certain words (false positives that surface as network failures). action='add' with `terms` to record word(s) the user says get blocked (e.g. after a network failure they suspect was a proxy block); action='remove' with `terms`; action='list' to show the current list and mode. Use 'add' when the user explicitly asks to avoid a word, or when a network failure recurs and the user identifies the likely trigger. The list is local; in `defang` mode (aiSharePoint.proxy.mode) listed words are auto-adjusted in outgoing messages to slip past the proxy.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "action": {
              "type": "string",
              "enum": [
                "add",
                "remove",
                "list"
              ],
              "description": "add/remove word(s), or list the current avoid-list."
            },
            "terms": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Word(s)/phrase(s) to add or remove (for add/remove)."
            }
          }
        }
      },
      {
        "name": "aisharepoint_capture_lesson",
        "displayName": "Capture Lesson Learned",
        "toolReferenceName": "spCaptureLesson",
        "canBeReferencedInPrompt": true,
        "icon": "$(book)",
        "tags": [
          "sharepoint",
          "context"
        ],
        "modelDescription": "Record a GENERALIZED, ANONYMIZED interaction lesson when you self-correct or discover a reusable pattern — so the plugin developer can bake it into a future release. Canonical example: you searched Confluence globally, then corrected to the user's PERSONAL space; the lesson is \"when the user says 'my Confluence space', scope to their personal space (~userid), not global.\" Write the heuristic in the ABSTRACT: NEVER include real space keys, usernames, site names, URLs, tenant names, page titles, or any tenant-specific value (they are scrubbed anyway, but write them out generically). Provide 'trigger' (the user situation it applies to) and 'lesson' (the better action). Categories: scoping, tool-selection, query-shape, interpretation, workflow, other. Use it sparingly for genuinely reusable insight, not per-turn detail. No-op unless the user opted in (aiSharePoint.lessons.capture); never claim a lesson was saved when it reports it was not stored.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "category": {
              "type": "string",
              "enum": [
                "scoping",
                "tool-selection",
                "query-shape",
                "interpretation",
                "workflow",
                "other"
              ],
              "description": "Which kind of lesson this is."
            },
            "trigger": {
              "type": "string",
              "description": "The generalized user situation/intent that should trigger this lesson (no tenant-specific values)."
            },
            "lesson": {
              "type": "string",
              "description": "The generalized, reusable heuristic — the better action to take (no tenant-specific values)."
            },
            "tags": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional short tags: tool names or source types (e.g. confluence, sharepoint, search_context)."
            }
          },
          "required": [
            "trigger",
            "lesson"
          ]
        }
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "aiSharePoint.addManagedTarget",
          "when": "view == aiSharePoint.sitesView",
          "group": "navigation@1"
        },
        {
          "command": "aiSharePoint.refreshSites",
          "when": "view == aiSharePoint.sitesView",
          "group": "navigation@2"
        },
        {
          "command": "aiSharePoint.showUsage",
          "when": "view == aiSharePoint.usageView",
          "group": "navigation@1"
        },
        {
          "command": "aiSharePoint.exportDiagnostics",
          "when": "view == aiSharePoint.supportView",
          "group": "navigation@1"
        },
        {
          "command": "aiSharePoint.reviewLessons",
          "when": "view == aiSharePoint.supportView",
          "group": "navigation@2"
        },
        {
          "command": "aiSharePoint.draftTeamsMessage",
          "when": "view == aiSharePoint.commsView",
          "group": "navigation@1"
        },
        {
          "command": "aiSharePoint.draftOutlookEmail",
          "when": "view == aiSharePoint.commsView",
          "group": "navigation@2"
        },
        {
          "command": "aiSharePoint.configureTeamsWebhook",
          "when": "view == aiSharePoint.commsView",
          "group": "1_config@1"
        },
        {
          "command": "aiSharePoint.testCommsMethod",
          "when": "view == aiSharePoint.commsView",
          "group": "1_config@2"
        },
        {
          "command": "aiSharePoint.addContextSource",
          "when": "view == aiSharePoint.sourcesView",
          "group": "navigation@1"
        },
        {
          "command": "aiSharePoint.clearContextCache",
          "when": "view == aiSharePoint.sourcesView",
          "group": "navigation@2"
        },
        {
          "command": "aiSharePoint.discoverActiveDirectory",
          "when": "view == aiSharePoint.sourcesView",
          "group": "navigation@3"
        },
        {
          "command": "aiSharePoint.exportReferenceConfig",
          "when": "view == aiSharePoint.sourcesView",
          "group": "1_share@1"
        },
        {
          "command": "aiSharePoint.importReferenceConfig",
          "when": "view == aiSharePoint.sourcesView",
          "group": "1_share@2"
        },
        {
          "command": "aiSharePoint.switchProject",
          "when": "view == aiSharePoint.sourcesView",
          "group": "navigation@0"
        },
        {
          "command": "aiSharePoint.createProject",
          "when": "view == aiSharePoint.projectsView",
          "group": "navigation@1"
        },
        {
          "command": "aiSharePoint.switchProject",
          "when": "view == aiSharePoint.projectsView",
          "group": "navigation@2"
        }
      ],
      "view/item/context": [
        {
          "command": "aiSharePoint.testConnection",
          "when": "(view == aiSharePoint.sitesView || view == aiSharePoint.sourcesView) && viewItem =~ /^site-/",
          "group": "inline@1"
        },
        {
          "command": "aiSharePoint.openSiteInBrowser",
          "when": "(view == aiSharePoint.sitesView || view == aiSharePoint.sourcesView) && viewItem =~ /^site-/",
          "group": "inline@2"
        },
        {
          "command": "aiSharePoint.testConnection",
          "when": "(view == aiSharePoint.sitesView || view == aiSharePoint.sourcesView) && viewItem =~ /^site-/",
          "group": "1_actions@1"
        },
        {
          "command": "aiSharePoint.copySiteUrl",
          "when": "(view == aiSharePoint.sitesView || view == aiSharePoint.sourcesView) && viewItem =~ /^site-/",
          "group": "1_actions@2"
        },
        {
          "command": "aiSharePoint.configureSiteRepo",
          "when": "view == aiSharePoint.sitesView && viewItem == site-managed",
          "group": "2_sync@1"
        },
        {
          "command": "aiSharePoint.pullSiteToRepo",
          "when": "view == aiSharePoint.sitesView && viewItem == site-managed",
          "group": "2_sync@2"
        },
        {
          "command": "aiSharePoint.pushSiteRepo",
          "when": "view == aiSharePoint.sitesView && viewItem == site-managed",
          "group": "2_sync@3"
        },
        {
          "command": "aiSharePoint.changeSiteRole",
          "when": "(view == aiSharePoint.sitesView || view == aiSharePoint.sourcesView) && viewItem =~ /^site-/",
          "group": "3_manage@1"
        },
        {
          "command": "aiSharePoint.signOutSite",
          "when": "(view == aiSharePoint.sitesView || view == aiSharePoint.sourcesView) && viewItem =~ /^site-/",
          "group": "3_manage@2"
        },
        {
          "command": "aiSharePoint.removeSite",
          "when": "(view == aiSharePoint.sitesView || view == aiSharePoint.sourcesView) && viewItem =~ /^site-/",
          "group": "3_manage@3"
        },
        {
          "command": "aiSharePoint.testContextSource",
          "when": "view == aiSharePoint.sourcesView && viewItem =~ /^context-source/",
          "group": "inline@1"
        },
        {
          "command": "aiSharePoint.testContextSource",
          "when": "view == aiSharePoint.sourcesView && viewItem =~ /^context-source/",
          "group": "1_actions@1"
        },
        {
          "command": "aiSharePoint.resetSourceLockout",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-source-locked",
          "group": "1_actions@2"
        },
        {
          "command": "aiSharePoint.removeContextSource",
          "when": "view == aiSharePoint.sourcesView && viewItem =~ /^context-source/",
          "group": "2_manage@1"
        },
        {
          "command": "aiSharePoint.addBookmark",
          "when": "view == aiSharePoint.sourcesView && viewItem =~ /^context-source/",
          "group": "1_actions@3"
        },
        {
          "command": "aiSharePoint.editBookmark",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-bookmark",
          "group": "inline@1"
        },
        {
          "command": "aiSharePoint.editBookmark",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-bookmark",
          "group": "1_actions@1"
        },
        {
          "command": "aiSharePoint.removeBookmark",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-bookmark",
          "group": "1_actions@2"
        },
        {
          "command": "aiSharePoint.testContextSource",
          "when": "view == aiSharePoint.sitesView && viewItem =~ /^managed-source/",
          "group": "inline@1"
        },
        {
          "command": "aiSharePoint.openSourceInBrowser",
          "when": "view == aiSharePoint.sitesView && viewItem =~ /^managed-source/",
          "group": "inline@2"
        },
        {
          "command": "aiSharePoint.testContextSource",
          "when": "view == aiSharePoint.sitesView && viewItem =~ /^managed-source/",
          "group": "1_actions@1"
        },
        {
          "command": "aiSharePoint.testWriteAccess",
          "when": "view == aiSharePoint.sitesView && viewItem == managed-source-confluence",
          "group": "1_actions@2"
        },
        {
          "command": "aiSharePoint.testConfluenceFunctionality",
          "when": "view == aiSharePoint.sitesView && viewItem == managed-source-confluence",
          "group": "1_actions@3"
        },
        {
          "command": "aiSharePoint.browseSource",
          "when": "view == aiSharePoint.sitesView && viewItem =~ /^managed-source/",
          "group": "1_actions@4"
        },
        {
          "command": "aiSharePoint.changeSourceRole",
          "when": "view == aiSharePoint.sitesView && viewItem =~ /^managed-source/",
          "group": "2_manage@1"
        },
        {
          "command": "aiSharePoint.removeContextSource",
          "when": "view == aiSharePoint.sitesView && viewItem =~ /^managed-source/",
          "group": "2_manage@2"
        },
        {
          "command": "aiSharePoint.applyRepoToSharePoint",
          "when": "view == aiSharePoint.sitesView && viewItem == site-managed",
          "group": "2_sync@4"
        },
        {
          "command": "aiSharePoint.revertSiteToCommit",
          "when": "view == aiSharePoint.sitesView && viewItem == site-managed",
          "group": "2_sync@5"
        },
        {
          "command": "aiSharePoint.browseSource",
          "when": "view == aiSharePoint.sourcesView && viewItem =~ /^context-source/",
          "group": "inline@2"
        },
        {
          "command": "aiSharePoint.browseSource",
          "when": "view == aiSharePoint.sourcesView && viewItem =~ /^context-source/",
          "group": "1_actions@2"
        },
        {
          "command": "aiSharePoint.exportSearchResults",
          "when": "view == aiSharePoint.sourcesView && viewItem =~ /^context-source/",
          "group": "1_actions@3"
        },
        {
          "command": "aiSharePoint.editSourceAlias",
          "when": "view == aiSharePoint.sourcesView && viewItem =~ /^context-source/",
          "group": "1_actions@4"
        },
        {
          "command": "aiSharePoint.loadSourceSchema",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-source-db",
          "group": "1_schema@1"
        },
        {
          "command": "aiSharePoint.precacheSourceCatalog",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-source-atlassian",
          "group": "1_schema@1"
        },
        {
          "command": "aiSharePoint.reviewCommDraft",
          "when": "view == aiSharePoint.commsView && viewItem == comm-draft",
          "group": "inline@1"
        },
        {
          "command": "aiSharePoint.editCommDraft",
          "when": "view == aiSharePoint.commsView && viewItem == comm-draft",
          "group": "inline@2"
        },
        {
          "command": "aiSharePoint.reviewCommDraft",
          "when": "view == aiSharePoint.commsView && viewItem == comm-draft",
          "group": "1_actions@1"
        },
        {
          "command": "aiSharePoint.editCommDraft",
          "when": "view == aiSharePoint.commsView && viewItem == comm-draft",
          "group": "1_actions@2"
        },
        {
          "command": "aiSharePoint.discardCommDraft",
          "when": "view == aiSharePoint.commsView && viewItem == comm-draft",
          "group": "1_actions@3"
        },
        {
          "command": "aiSharePoint.indexSourceSchema",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-source-db",
          "group": "1_schema@2"
        },
        {
          "command": "aiSharePoint.indexSourceContent",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-source-db",
          "group": "1_schema@3"
        },
        {
          "command": "aiSharePoint.buildErDiagram",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-source-db",
          "group": "1_schema@4"
        },
        {
          "command": "aiSharePoint.viewSourceSchema",
          "when": "view == aiSharePoint.sourcesView && viewItem == context-source-db",
          "group": "1_schema@5"
        },
        {
          "command": "aiSharePoint.clearErrorReports",
          "when": "view == aiSharePoint.supportView && viewItem == errorReports",
          "group": "inline@1"
        },
        {
          "command": "aiSharePoint.clearErrorReports",
          "when": "view == aiSharePoint.supportView && viewItem == errorReports",
          "group": "1_actions@1"
        },
        {
          "command": "aiSharePoint.editProject",
          "when": "view == aiSharePoint.projectsView && viewItem =~ /^project/",
          "group": "1_actions@1"
        },
        {
          "command": "aiSharePoint.manageProjectAiContext",
          "when": "view == aiSharePoint.projectsView && viewItem =~ /^project/",
          "group": "1_actions@2"
        },
        {
          "command": "aiSharePoint.removeProject",
          "when": "view == aiSharePoint.projectsView && viewItem =~ /^project/",
          "group": "2_manage@1"
        }
      ],
      "commandPalette": [
        {
          "command": "aiSharePoint.refreshSites",
          "when": "false"
        },
        {
          "command": "aiSharePoint.openSiteInBrowser",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.copySiteUrl",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.changeSiteRole",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.signOutSite",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.removeSite",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.testConnection",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.configureSiteRepo",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.pullSiteToRepo",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.pushSiteRepo",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.runBookmark",
          "when": "false"
        },
        {
          "command": "aiSharePoint.removeBookmark",
          "when": "false"
        },
        {
          "command": "aiSharePoint.applyRepoToSharePoint",
          "when": "aiSharePoint.hasSites"
        },
        {
          "command": "aiSharePoint.revertSiteToCommit",
          "when": "aiSharePoint.hasSites"
        }
      ]
    },
    "walkthroughs": [
      {
        "id": "aiSharePoint.gettingStarted",
        "title": "Get started with AI SharePoint",
        "description": "Connect a site, chat with @sharepoint, stay in control of Copilot spend, and know exactly what leaves your machine (nothing, unless you export it).",
        "steps": [
          {
            "id": "installCopilot",
            "title": "Install GitHub Copilot Chat",
            "description": "AI features run through your own Copilot entitlement via the VS Code Language Model API — provided by the **GitHub Copilot Chat** extension (marketplace search: “github copilot chat”). No extra keys, nothing routed through third parties.\nThis step completes automatically when Copilot Chat is installed and you are signed in.\n[Install GitHub Copilot Chat](command:aiSharePoint.installCopilotChat)\n[Check Copilot Status](command:aiSharePoint.checkCopilotStatus)",
            "media": {
              "image": "media/walkthrough/copilot.svg",
              "altText": "Copilot powers AI SharePoint through your own entitlement"
            },
            "completionEvents": [
              "extensionInstalled:GitHub.copilot-chat",
              "onContext:aiSharePoint.copilotSignedIn"
            ]
          },
          {
            "id": "connectSite",
            "title": "Connect a SharePoint site",
            "description": "Sign in with your Microsoft account (browser or device-code) and connect a site as **managed** or read-only **reference**. Credentials are cached only in your OS keychain.\n[Connect a Site](command:aiSharePoint.connectSite)",
            "media": {
              "image": "media/walkthrough/connect.svg",
              "altText": "Connect a SharePoint site"
            },
            "completionEvents": [
              "onCommand:aiSharePoint.connectSite"
            ]
          },
          {
            "id": "chat",
            "title": "Chat with @sharepoint",
            "description": "Ask **@sharepoint** about your sites in the Chat view — try `/site` for a live overview. In agent mode, Copilot can call the read-only tools itself: site overviews, pages, reference sources (Confluence/Jira/LDAP), bookmarks, and usage.\n[Open Chat](command:workbench.action.chat.open?%22%40sharepoint%20%2Fhelp%22)",
            "media": {
              "image": "media/walkthrough/chat.svg",
              "altText": "Chat with @sharepoint"
            },
            "completionEvents": [
              "onCommand:workbench.action.chat.open"
            ]
          },
          {
            "id": "projects",
            "title": "Organize work into Projects",
            "description": "The **Projects** view groups reference sources into a named scope, carries your **goals & instructions** for @sharepoint, and keeps a separate **AI-managed context** the assistant learns over time (you approve each saved note). Activate a project to scope chat and the Reference Sources view to it — everything travels with Export/Import Reference Config.\n[Open the Projects View](command:aiSharePoint.projectsView.focus)\n[Create a Project](command:aiSharePoint.createProject)",
            "media": {
              "image": "media/walkthrough/chat.svg",
              "altText": "The Projects view"
            },
            "completionEvents": [
              "onCommand:aiSharePoint.createProject"
            ]
          },
          {
            "id": "activity",
            "title": "See what the extension asked Copilot",
            "description": "The Copilot Activity view and dashboard show factual, locally measured request counts (today, this month, by model and task). Premium-request consumption against your plan is **not** estimated — your GitHub billing page is the authoritative source.\n[Open Activity Dashboard](command:aiSharePoint.showUsage)",
            "media": {
              "image": "media/walkthrough/budget.svg",
              "altText": "Copilot activity counters"
            },
            "completionEvents": [
              "onCommand:aiSharePoint.showUsage"
            ]
          },
          {
            "id": "diagnostics",
            "title": "Diagnostics that respect your enterprise",
            "description": "Usage counters and redacted error reports are stored **locally only**. When you need support, export an **anonymized bundle** — previewed and leak-scanned before a single byte is written.\n[Export Diagnostics Bundle](command:aiSharePoint.exportDiagnostics)\n[Privacy & Data Notice](command:aiSharePoint.openPrivacyNotice)",
            "media": {
              "image": "media/walkthrough/diagnostics.svg",
              "altText": "Anonymized diagnostics export"
            },
            "completionEvents": [
              "onCommand:aiSharePoint.exportDiagnostics"
            ]
          }
        ]
      }
    ],
    "configuration": {
      "title": "AI SharePoint",
      "properties": {
        "aiSharePoint.copilot.preferredModelFamily": {
          "type": "string",
          "default": "",
          "order": 2,
          "markdownDescription": "Model family this extension uses by default (set via **AI SharePoint: List Copilot Models**). Empty = cheapest entitled model (economy-first policy)."
        },
        "aiSharePoint.auth.tenantAuthority": {
          "type": "string",
          "default": "https://login.microsoftonline.com/common",
          "scope": "machine",
          "order": 6,
          "markdownDescription": "Microsoft Entra ID authority for sign-in (MSAL public client). Machine-scoped: workspaces cannot override it. The host must be a known Microsoft login endpoint or listed in `#aiSharePoint.auth.additionalAuthorityHosts#`."
        },
        "aiSharePoint.auth.clientId": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "order": 7,
          "markdownDescription": "Optional custom Entra application (client) ID for tenants that block the default Microsoft Graph PowerShell first-party app. Requires a public-client app registration with `http://localhost` redirect and delegated `Sites.Read.All` — see the Admin Guide."
        },
        "aiSharePoint.auth.additionalAuthorityHosts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "scope": "machine",
          "order": 8,
          "markdownDescription": "Extra allowed sign-in authority hosts (beyond the built-in Microsoft login endpoints). Machine-scoped."
        },
        "aiSharePoint.diagnostics.usageCapture": {
          "type": "string",
          "enum": [
            "followVSCode",
            "on",
            "off"
          ],
          "enumDescriptions": [
            "Capture local usage counters only while VS Code telemetry is enabled (defers to your org's telemetry stance).",
            "Always capture locally.",
            "Never capture."
          ],
          "default": "followVSCode",
          "order": 9,
          "markdownDescription": "Local feature-usage counters for the diagnostics bundle. **Nothing is ever transmitted by the extension** — data leaves the machine only via your explicit export."
        },
        "aiSharePoint.diagnostics.errorCapture": {
          "type": "boolean",
          "default": true,
          "order": 10,
          "markdownDescription": "Store redacted error reports locally for the diagnostics bundle. Disable to keep no error history at all."
        },
        "aiSharePoint.lessons.capture": {
          "type": "boolean",
          "default": false,
          "order": 10.5,
          "markdownDescription": "**Opt-in, default off.** Let @sharepoint record *anonymized, generalized* interaction lessons when it self-corrects (e.g. \"when the user says 'my Confluence space', scope to their personal space, not global\") into a local ledger you can review and export. The lessons are scrubbed of tenant/user identifiers and **nothing is ever transmitted by the extension** — they leave the machine only when you explicitly export the file to share with the plugin developer (\"AI SharePoint: Export Lessons Learned\")."
        },
        "aiSharePoint.sync.allowedRemoteHosts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "github.com"
          ],
          "scope": "machine",
          "order": 11,
          "markdownDescription": "Git hosts that site repositories may push to (ADR-0019). Add your GitHub Enterprise Server host, e.g. `github.corp.example`. Machine-scoped: workspaces cannot override it."
        },
        "aiSharePoint.sync.writePermissionMode": {
          "type": "string",
          "enum": [
            "selected",
            "all"
          ],
          "enumDescriptions": [
            "Sites.Selected (recommended, least privilege): an admin grants the app access to ONLY the specific site(s). Requires a one-time per-site grant — see the Admin Guide.",
            "Sites.ReadWrite.All + Sites.Manage.All: tenant-wide delegated write across every site you can access. Simpler, but usually needs broad admin consent."
          ],
          "default": "selected",
          "order": 11,
          "markdownDescription": "Graph permission the write-back (*Apply to SharePoint*) requests (ADR-0037). **`selected`** asks for `Sites.Selected` — far more likely to be approved, but an admin must grant the app access to each target site (`Grant-PnPAzureADAppSitePermission`, or `POST /sites/{id}/permissions`; see the Admin Guide §4). **`all`** asks for the tenant-wide `Sites.ReadWrite.All` + `Sites.Manage.All`."
        },
        "aiSharePoint.context.cacheTtlMinutes": {
          "type": "number",
          "default": 15,
          "minimum": 0,
          "order": 12,
          "markdownDescription": "How long reference-source results are cached locally (ADR-0011). 0 disables caching."
        },
        "aiSharePoint.context.maxResults": {
          "type": "number",
          "default": 25,
          "minimum": 1,
          "maximum": 200,
          "order": 13,
          "markdownDescription": "Default result window — the maximum rows/results per reference-source query (ADR-0012 read-safety cap). Tunable mid-conversation by asking @sharepoint to change the result window; the 200 ceiling always applies."
        },
        "aiSharePoint.context.queryTimeoutSeconds": {
          "type": "number",
          "default": 30,
          "minimum": 5,
          "maximum": 600,
          "order": 13,
          "markdownDescription": "Base timeout for a single database query/probe (seconds). Large join probes are automatically granted a longer, cost-estimated budget (up to ~2 minutes, or this value if higher) based on table row counts, so big joins finish instead of timing out at the default."
        },
        "aiSharePoint.context.maxInputTokens": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "order": 13,
          "markdownDescription": "Override the model's advertised input-context size (tokens) when Copilot clamps the effective usable context below the native max (#3). `0` = auto-detect: use the model's advertised limit and learn the real ceiling over time from successes and overflow failures. Set a value only if you know your environment's hard clamp."
        },
        "aiSharePoint.proxy.mode": {
          "type": "string",
          "enum": [
            "off",
            "warn",
            "defang"
          ],
          "enumDescriptions": [
            "No proxy-block avoidance.",
            "Flag avoid-list words in your message before sending (you rephrase).",
            "Automatically insert an invisible separator into avoid-list words so a content-blocking proxy's match fails, while the model still reads them."
          ],
          "default": "warn",
          "order": 14,
          "markdownDescription": "How @sharepoint handles words a corporate proxy may block (#4). `warn` flags them; `defang` auto-adjusts outgoing messages to slip past; `off` disables. Repeated network-level failures also trigger proxy-block guidance regardless of this setting."
        },
        "aiSharePoint.proxy.blockedTerms": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "order": 14,
          "markdownDescription": "Words/phrases a corporate proxy or DLP appliance tends to block (often false positives on ordinary vocabulary). @sharepoint avoids them per `aiSharePoint.proxy.mode`. You can also grow this list at runtime by asking @sharepoint to add a word, or via \"AI SharePoint: Manage Proxy Avoid-List\"."
        },
        "aiSharePoint.context.catalogTtlHours": {
          "type": "number",
          "default": 24,
          "minimum": 1,
          "order": 13,
          "markdownDescription": "How long a pre-cached source catalog (Confluence spaces / Jira projects+queues) stays fresh. After expiry the next browse offers a refresh; the expired copy remains usable."
        },
        "aiSharePoint.context.catalogCheckpointSeconds": {
          "type": "number",
          "default": 15,
          "minimum": 5,
          "order": 13,
          "markdownDescription": "While pre-caching a large catalog, ask every N seconds whether to continue. Loading pauses while the prompt waits, so the source system is never overtaxed."
        },
        "aiSharePoint.context.allowSchemaIndexing": {
          "type": "boolean",
          "default": true,
          "order": 13,
          "markdownDescription": "Allow building **semantic schema indexes** of database sources with Copilot (ADR-0024). Only table/column **names and types** are ever sent — never row data — and always behind an explicit per-source consent. Set `false` to disable organization-wide."
        },
        "aiSharePoint.ldap.tlsRejectUnauthorized": {
          "type": "boolean",
          "default": true,
          "order": 14,
          "markdownDescription": "Validate LDAPS/StartTLS server certificates for LDAP/Active Directory sources (ADR-0020). Keep **true** in production. If LDAPS uses an internal CA not in Node's bundle, prefer installing the CA; only disable for isolated lab testing."
        },
        "aiSharePoint.ldap.useStartTls": {
          "type": "boolean",
          "default": false,
          "order": 15,
          "markdownDescription": "For `ldap://` (port 389) sources, upgrade the connection with StartTLS before binding. Ignored for `ldaps://` (implicit TLS)."
        },
        "aiSharePoint.ldap.caCertificatesFile": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "order": 16,
          "markdownDescription": "Path to a PEM CA bundle trusted for LDAPS/StartTLS connections (e.g. your corporate root + intermediates), **appended** to Node's defaults and the OS trust store. Machine-scoped. Use when the OS-store API is unavailable on your VS Code runtime — see Admin Guide §7."
        },
        "aiSharePoint.logging.verboseWire": {
          "type": "boolean",
          "default": false,
          "order": 30,
          "markdownDescription": "Log the **full request/response detail of every integration** — Microsoft Graph (SharePoint, Teams, Outlook), Confluence/Jira, LDAP/Active Directory, SQL Server/PostgreSQL/MySQL/MongoDB, Vertex AI Search, Power BI, MSAL sign-in traffic, and Copilot prompts/responses — to the *AI SharePoint* output channel. **Secrets are redacted in layers**: authorization headers are reduced to their scheme, token-endpoint bodies are withheld entirely, credential-shaped fields are masked, and every line passes the global redaction filter. Database/LDAP **result data is summarized (counts/columns), not dumped**. Local only; never included in diagnostics exports. Expect large output — enable only while debugging."
        },
        "aiSharePoint.ldap.dnsServers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "order": 16,
          "markdownDescription": "**Machine-scoped.** Internal DNS server IPs used for Active Directory **SRV lookups** (e.g. `[\"10.0.0.53\", \"10.0.1.53\"]`). Set this when LDAP works in the office but fails on VPN: Node's resolver bypasses Windows NRPT / VPN split-DNS rules, so pointing SRV queries directly at corporate DNS by IP makes discovery deterministic everywhere. Empty = system resolver."
        },
        "aiSharePoint.servicenow.oauthClientId": {
          "type": "string",
          "default": "",
          "order": 17,
          "markdownDescription": "**Machine-scoped.** OAuth client ID for **ServiceNow browser sign-in** — from a one-time Application Registry entry (System OAuth → *Create an OAuth API endpoint for external clients*) with redirect URL `http://localhost:51725/callback`. Users then sign in via their browser SSO session; no passwords or hand-issued tokens. Leave empty to hide the browser option."
        }
      }
    }
  },
  "scripts": {
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node scripts/run-tests.js",
    "check:native": "node scripts/check-native-deps.js",
    "scan:secrets": "node scripts/scan-secrets.js",
    "build:samples": "tsc -p tsconfig.test.json && node scripts/render-dashboard-sample.js",
    "package": "vsce package --no-dependencies --no-rewrite-relative-links --allow-missing-repository",
    "vscode:prepublish": "node esbuild.js --production"
  },
  "dependencies": {
    "@azure/msal-node": "^5.2.4",
    "ldapts": "^8.1.8",
    "mongodb": "^7.3.0",
    "mysql2": "^3.22.5",
    "pg": "^8.21.0",
    "tedious": "^19.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@types/pg": "^8.20.0",
    "@types/vscode": "^1.95.0",
    "@vscode/vsce": "^3.2.1",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}
